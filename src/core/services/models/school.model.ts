export type ISchoolInfo = {
    id: string;
    title: string;
    code: string;
    address: string;
    email: string;
    phone: string;
    logoUrl?: string;
    description?: string;
    createdAt?: string;
    updatedAt?: string;
};
